# Matcher fixture corpus

These cases are independently authored from the matching contract in
`_private/design.md`. They contain only synthetic names, paths, sizes, identities,
and expected stable outcomes. No cross-seed or qui source code or derived fixture
data was consulted.

The compact fixture schema deliberately describes public behavior rather than the
matcher's internal scoring. Unless specified otherwise, torrents have different
identities and all matching modes are enabled.
